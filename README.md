# Expense Tracker

A simple C++ program to track daily expenses.

## Features
- Add, view, and delete expenses
- Calculate total spending
- Save data locally

## How to Run
1. Open the project in VS Code
2. Compile with `g++ main.cpp -o ExpenseTracker`
3. Run with `./ExpenseTracker`